<Window x:Class="PostirushkiApp.Views.Windows.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PostirushkiApp.Views.Windows"
        mc:Ignorable="d"
        Title="AdminWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock VerticalAlignment="Center"
                   HorizontalAlignment="Center" 
                   FontSize="10"
                   FontWeight="Medium"
                   Text="Администратор"
                   Margin="10 20 0 0"></TextBlock>
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin=" 0 0 20 10">
            <Button Content="Добавить сотрудника" Name="btnAddUser" Click="btnAddUser_Click" Width="150"></Button>
        </StackPanel>

        <Grid Grid.Row="1" Margin="20 0 20 20">
            <Frame Name="MainFrame" NavigationUIVisibility="Hidden"></Frame>

        </Grid>
    </Grid>
</Window>
